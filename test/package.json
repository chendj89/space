{
  "name": "test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "rimraf": "^3.0.2",
    "vitest": "^0.23.4"
  },
  "scripts": {
    "clean": "rimraf clone",
    "vitest": "npm run clean && vitest --config ./vitest.config.ts run && npm run clean"
  }
}
